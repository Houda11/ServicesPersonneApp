@model IEnumerable<ApplicationServicePersonne.Models.Historique_client>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_note)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_hist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_client)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employeur.nom_employeur)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_note)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_hist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_client)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employeur.nom_employeur)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_historique }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_historique }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_historique })
        </td>
    </tr>
}

</table>
