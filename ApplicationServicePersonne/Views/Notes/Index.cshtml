@model IEnumerable<ApplicationServicePersonne.Models.Note>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.valeur)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_client)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employeur.nom_employeur)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type_note.nom_type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.valeur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_client)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employeur.nom_employeur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type_note.nom_type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_note }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_note }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_note })
        </td>
    </tr>
}

</table>
